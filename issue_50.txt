"## Overview\n\nImplement the Stock Alert System module for configurable, rule-based inventory monitoring. This module supports creating alert rules for various conditions (low stock, high stock, expiring lots, slow-moving, overstock), evaluating those rules against current data, generating alerts with severity levels, and tracking alert lifecycle through acknowledgment and resolution.\n\n## Directory\n\n```\nsrc/inventory/_060_stock_alert/\n\u251c\u2500\u2500 __init__.py\n\u251c\u2500\u2500 models.py\n\u251c\u2500\u2500 schemas.py\n\u251c\u2500\u2500 service.py\n\u251c\u2500\u2500 router.py\n\u251c\u2500\u2500 validators.py\n\u251c\u2500\u2500 README.md\n\u2514\u2500\u2500 tests/\n    \u251c\u2500\u2500 test_models.py\n    \u251c\u2500\u2500 test_service.py\n    \u251c\u2500\u2500 test_router.py\n    \u2514\u2500\u2500 test_validators.py\n```\n\n## Import Rules\n\n```python\nfrom shared.types import Base, BaseModel, BaseSchema, PaginatedResponse, AuditableMixin, SoftDeleteMixin, Money, Address, ContactInfo, DocumentStatus, TaxType, PaymentStatus, Priority, Quantity, DateRange, CodeGenerator\nfrom shared.errors import NotFoundError, ValidationError, DuplicateError, BusinessRuleError, CalculationError\nfrom shared.schema import ColLen, Precision\nfrom src.foundation._001_database.engine import get_db\n```\n\n- ALL public functions: type hints + docstrings\n- Router prefix: `/api/v1/stock-alert`\n- Tests: pytest-asyncio\n- Only import from `shared/` and `_001_database`\n\n## Models\n\n### `AlertRule` (table: `alert_rules`)\n\n| Field | Type | Constraints |\n|---|---|---|\n| id | Integer | PK, auto-increment |\n| name | String(200) | NOT NULL |\n| alert_type | Enum[low_stock, high_stock, expiring_lot, slow_moving, over_stock] | NOT NULL |\n| product_code | String(50) | NULLABLE |\n| product_category | String(100) | NULLABLE |\n| warehouse_code | String(50) | NULLABLE |\n| threshold_value | Numeric(15,3) | NOT NULL |\n| threshold_operator | Enum[less_than, greater_than, equals] | NOT NULL |\n| comparison_field | Enum[quantity, value_days_of_supply] | NOT NULL |\n| is_active | Boolean | DEFAULT True |\n| notify_users | JSON | NOT NULL, list of user identifiers |\n| created_at | DateTime | server default |\n| updated_at | DateTime | server default, auto-update |\n\n### `StockAlert` (table: `stock_alerts`)\n\n| Field | Type | Constraints |\n|---|---|---|\n| id | Integer | PK, auto-increment |\n| rule_id | Integer | FK -> alert_rules.id |\n| product_code | String(50) | NOT NULL |\n| warehouse_code | String(50) | NOT NULL |\n| current_value | Numeric(15,3) | NOT NULL |\n| threshold_value | Numeric(15,3) | NOT NULL |\n| alert_type | Enum[low_stock, high_stock, expiring_lot, slow_moving, over_stock] | NOT NULL |\n| severity | Enum[info, warning, critical] | NOT NULL |\n| status | Enum[active, acknowledged, resolved] | NOT NULL |\n| message | Text | NOT NULL |\n| generated_at | DateTime | server default |\n| acknowledged_at | DateTime | NULLABLE |\n| resolved_at | DateTime | NULLABLE |\n| acknowledged_by | String(100) | NULLABLE |\n\n## Schemas\n\n| Schema Name | Purpose |\n|---|---|\n| `AlertRuleCreate` | Request body for creating an alert rule |\n| `AlertRuleResponse` | Full alert rule response |\n| `StockAlertResponse` | Stock alert with severity and status info |\n| `AlertAcknowledge` | Request body for acknowledging an alert |\n| `AlertStats` | Statistics: count by type, severity, resolution times |\n\n## Service Functions\n\n```python\nasync def create_rule(db: AsyncSession, data: AlertRuleCreate) -> AlertRule:\n    \"\"\"Create a new alert rule configuration.\"\"\"\n\nasync def update_rule(db: AsyncSession, rule_id: int, data: dict) -> AlertRule:\n    \"\"\"Update an existing alert rule.\"\"\"\n\nasync def evaluate_rules(db: AsyncSession, warehouse_code: str | None = None) -> list[StockAlert]:\n    \"\"\"Evaluate all active rules against current stock data and generate alerts.\"\"\"\n\nasync def get_alerts(db: AsyncSession, status: str | None = None, alert_type: str | None = None, severity: str | None = None, page: int = 1, page_size: int = 50) -> PaginatedResponse:\n    \"\"\"List stock alerts with optional filters and pagination.\"\"\"\n\nasync def acknowledge_alert(db: AsyncSession, alert_id: int, acknowledged_by: str) -> StockAlert:\n    \"\"\"Acknowledge an active alert, recording who acknowledged it.\"\"\"\n\nasync def resolve_alert(db: AsyncSession, alert_id: int) -> StockAlert:\n    \"\"\"Resolve an alert, setting resolved_at timestamp.\"\"\"\n\nasync def get_stats(db: AsyncSession, warehouse_code: str | None = None) -> AlertStats:\n    \"\"\"Get alert statistics: count by type, severity, average resolution time.\"\"\"\n\nasync def cleanup_old_alerts(db: AsyncSession, days: int = 90) -> int:\n    \"\"\"Delete resolved alerts older than the specified number of days. Returns count deleted.\"\"\"\n```\n\n## Router Endpoints\n\n| Method | Path | Description |\n|---|---|---|\n| POST | `/api/v1/stock-alert/alert-rules` | Create an alert rule |\n| GET | `/api/v1/stock-alert/alert-rules` | List alert rules |\n| PUT | `/api/v1/stock-alert/alert-rules/{id}` | Update an alert rule |\n| POST | `/api/v1/stock-alert/stock-alerts/evaluate` | Evaluate all rules and generate alerts |\n| GET | `/api/v1/stock-alert/stock-alerts` | List stock alerts |\n| POST | `/api/v1/stock-alert/stock-alerts/{id}/acknowledge` | Acknowledge an alert |\n| POST | `/api/v1/stock-alert/stock-alerts/{id}/resolve` | Resolve an alert |\n| GET | `/api/v1/stock-alert/stock-alerts/stats` | Get alert statistics |\n| DELETE | `/api/v1/stock-alert/stock-alerts/cleanup` | Cleanup old resolved alerts |\n\n## Validators\n\nIn `validators.py`:\n\n1. **threshold_value >= 0**: Threshold must be non-negative (raises `ValidationError`)\n2. **Valid operator**: threshold_operator must be one of the enum values (raises `ValidationError`)\n3. **notify_users not empty**: At least one user must be specified for notifications (raises `ValidationError`)\n4. **Stats calculation**: count by type (group by alert_type), count by severity (group by severity), average resolution time = avg(resolved_at - generated_at) for resolved alerts\n5. **Severity determination logic**: critical if below safety stock, warning if below reorder point, info otherwise\n\n## Tests\n\n### `tests/test_models.py`\n- Test AlertRule model creation with all fields\n- Test StockAlert model creation with FK relationship\n- Test default values for is_active, status\n- Test nullable fields on AlertRule (product_code, product_category, warehouse_code)\n\n### `tests/test_service.py`\n- Test create_rule, update_rule\n- Test evaluate_rules generates correct alerts based on thresholds\n- Test get_alerts with various filters (status, alert_type, severity)\n- Test acknowledge_alert sets acknowledged_at and acknowledged_by\n- Test resolve_alert sets resolved_at and changes status\n- Test get_stats returns correct counts and averages\n- Test cleanup_old_alerts deletes only old resolved alerts\n\n### `tests/test_router.py`\n- Test all endpoints return correct status codes\n- Test POST /alert-rules returns 201\n- Test POST /stock-alerts/evaluate triggers evaluation\n- Test POST /stock-alerts/{id}/acknowledge with request body\n- Test DELETE /stock-alerts/cleanup with days query param\n- Test error responses for invalid data (422)\n\n### `tests/test_validators.py`\n- Test threshold_value non-negative validation\n- Test threshold_operator enum validation\n- Test notify_users non-empty validation\n- Test severity determination logic with sample data\n- Test stats calculation with multiple alerts\n\n## Dependencies\n\n- SQLAlchemy 2.0 (async)\n- FastAPI\n- Pydantic v2\n- pytest-asyncio\n- `shared.types` for base classes\n- `shared.errors` for error classes\n- `shared.schema` for ColLen, Precision\n\n## Quality Checklist\n\n- [ ] All models have proper type hints and column constraints\n- [ ] All service functions have docstrings and type hints\n- [ ] Router uses async endpoints with proper response models\n- [ ] Validators raise appropriate custom errors\n- [ ] FK relationship between StockAlert and AlertRule is properly configured\n- [ ] Alert lifecycle: active -> acknowledged -> resolved works correctly\n- [ ] All tests pass with pytest-asyncio\n- [ ] No imports from outside `shared/` and `_001_database`\n- [ ] Pagination supported on list endpoints\n- [ ] Proper HTTP status codes (201 for create, 404 for not found, 422 for validation)\n"
